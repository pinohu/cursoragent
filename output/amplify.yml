version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd output
            - npm ci
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next/static
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: output 