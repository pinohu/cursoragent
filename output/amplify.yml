version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --prefix output ci
    build:
      commands:
        - npm --prefix output run build
  artifacts:
    baseDirectory: output/out
    files:
      - '**/*'
  cache:
    paths:
      - output/node_modules/**/* 